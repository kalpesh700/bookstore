{
  "name": "bookstore",
  "version": "1.0.0",
  "description": "Fullstack bookstore application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "concurrently \"npm run build --prefix frontend\" \"npm run build --prefix backend\"",
    "start": "npm run start --prefix backend"
  },
  "author": "Kalpesh Koli",
  "license": "ISC",
  "dependencies": {
    "daisyui": "^4.12.10",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "react-refresh": "^0.10.0",
    "vite": "^5.4.3"
  }
}
